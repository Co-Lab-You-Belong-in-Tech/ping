import Logo from "../assets/Logo.png";

const Signup = () => {
  return (
    <div>
      <div>
        <img src={Logo} />
      </div>
      Back to <a href="/">login.</a>
    </div>
  );
};

export default Signup;
